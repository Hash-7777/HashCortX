{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "HashCortx capability set. The app's own commands are registered via invoke_handler and need no entry here; only plugin APIs do, and only the ones the app calls: window placement, opening a web link in the system browser, and the open and save dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-outer-position",
    "core:window:allow-set-position",
    "core:window:allow-inner-size",
    "core:window:allow-set-size",
    "core:window:allow-center",
    "core:window:allow-show",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    "dialog:default",
    "dialog:allow-open"
  ]
}
